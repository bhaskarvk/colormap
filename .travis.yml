# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
sudo: required
dist: trusty
addons:
  apt:
    packages:
      - libv8-3.14-dev
r_binary_packages:
  - V8
  - stringr
  - ggplot2
  - scales
  - testthat
